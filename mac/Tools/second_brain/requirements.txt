# second-brain skill dependencies
#   pip install -r requirements.txt

# Optional: faster fuzzy search. The embedded skill has a standard-library fallback.
rapidfuzz>=3.0

# Optional: S3 backend. Local folders do not need it.
# Harmless to install for local-only use; remove this line if you never use S3.
boto3>=1.34
